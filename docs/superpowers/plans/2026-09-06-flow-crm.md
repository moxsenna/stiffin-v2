# Ralivo Flow CRM Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tim sales mendapat feedback loop cepat pasca-chat WA (taksonomi outcome), gambaran besar pipeline via Kanban, catatan cepat per prospek, tombol kalender, template dengan 3 persona, filter kontak cerdas, kalkulator omset/komisi di header, dan memastikan aftercare D+7 otomatis berjalan.

**Architecture:** Semua perubahan di `apps/promotor-flow-web` (UI + module ports/commands existing) dan `apps/platform-api/src/routes/flow-routes.ts` + services. Prinsip: tidak mengganti mesin `next_actions` / `contact_lifecycle` — fitur baru hanya **memakai** `createNextAction`, `transitionStage`, dan `activities` yang sudah ada. Util kalender bersama di `packages/platform-core` (pure, tanpa dependency).

**Tech Stack:** Next.js 15 client components (CSS tokens), Hono, Drizzle, Zod contracts, HTML5 native drag-and-drop (tanpa lib dnd), `node:test` via `tsx --test`.

## Global Constraints

- Copy Bahasa Indonesia hardcoded; styling tokens + utility existing (`BottomSheet`, `SegmentedControl`, `useToast`, `EmptyState`); tanpa Tailwind; tanpa lib dnd/kalender baru.
- Domain logic murni di `apps/platform-api/src/domain/` atau `packages/platform-core` agar bisa dites tanpa DB.
- Stage contact hanya berubah via `contactLifecycleService.transitionStage` (aturan LOST wajib reason, dsb).
- Uang integer IDR; timezone default `Asia/Jakarta` (`DEFAULT_ORGANIZATION_TIMEZONE`).
- Migrasi via `db:generate` + `db:migrate`; `pnpm typecheck` sebelum commit; commit konvensional.
- Test runner `node:test`; integrasi butuh `TEST_DATABASE_URL`.

---

### Task C1: Post-WhatsApp Action Modal (Taksonomi Outcome)

**Files:**
- Modify: `packages/contracts/src/index.ts` (`ConfirmWhatsAppSentRequestSchema` + enum outcome)
- Modify: `apps/platform-api/src/domain/next-action-rules.ts` (mapping outcome → aturan)
- Modify: `apps/platform-api/src/services/next-action-service.ts` (handle outcome saat complete)
- Modify: `apps/platform-api/src/routes/flow-routes.ts:581` (teruskan outcome)
- Modify: `apps/promotor-flow-web/src/components/today/WhatsAppBottomSheet.tsx`
- Modify: `apps/promotor-flow-web/src/modules/messaging/commands.ts` + pemanggil (`TodayPage`, `ContactDetailPage`)
- Test: `apps/platform-api/src/__tests__/integration/wa-outcome.integration.test.ts`, `apps/promotor-flow-web/src/__tests__/wa-outcome.test.ts`

**Interfaces:**
- Produces:
  - `ContactWaOutcome = 'INTERESTED_TEST' | 'ASK_SCHEDULE' | 'WAIT_PAYDAY' | 'NO_RESPONSE'`.
  - `ConfirmWhatsAppSentRequestSchema` + `outcome?: ContactWaOutcome`.
  - Perilaku backend saat confirm dengan outcome:
    | Outcome | Efek |
    |---|---|
    | `INTERESTED_TEST` | stage → `INTERESTED` (via lifecycle service) + NextAction `FOLLOW_UP` "Ajak booking Tes STIFIn" due +2 hari, priority 70, idempotency `wa-outcome:INTERESTED_TEST:{actionId}` |
    | `ASK_SCHEDULE` | NextAction `MANUAL` "Kunci jadwal konsultasi" due +1 hari, priority 80, idempotency `wa-outcome:ASK_SCHEDULE:{actionId}` |
    | `WAIT_PAYDAY` | next follow-up +3 hari (mengabaikan pilihan manual bila tidak diisi) |
    | `NO_RESPONSE` | next follow-up +2 hari (default) |
  - Response confirm memuat `createdAction?: { id, title, dueAt } | null`.
  - UI: `onConfirmSent(scheduleNextDays?, outcome?)`.

- [ ] **Step 1: Unit test mapping yang gagal**

```ts
// apps/platform-api/src/__tests__/integration/wa-outcome.integration.test.ts
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { resolveOutcomeEffect } from '../domain/next-action-rules';

describe('resolveOutcomeEffect', () => {
  it('INTERESTED_TEST → stage INTERESTED + follow-up +2 hari', () => {
    const effect = resolveOutcomeEffect('INTERESTED_TEST', 'act-1', new Date('2026-09-06T02:00:00Z'));
    assert.equal(effect.stage, 'INTERESTED');
    assert.equal(effect.followUp?.title, 'Ajak booking Tes STIFIn');
    assert.equal(effect.followUp?.dueAt.toISOString(), new Date('2026-09-08T02:00:00Z').toISOString());
    assert.equal(effect.followUp?.idempotencyKey, 'wa-outcome:INTERESTED_TEST:act-1');
  });

  it('ASK_SCHEDULE → manual action kunci jadwal +1 hari', () => {
    const effect = resolveOutcomeEffect('ASK_SCHEDULE', 'act-1', new Date('2026-09-06T02:00:00Z'));
    assert.equal(effect.followUp?.actionType, 'MANUAL');
    assert.equal(effect.followUp?.title, 'Kunci jadwal konsultasi');
  });

  it('WAIT_PAYDAY → follow-up +3 hari', () => {
    const effect = resolveOutcomeEffect('WAIT_PAYDAY', 'act-1', new Date('2026-09-06T02:00:00Z'));
    assert.equal(effect.nextFollowUpDays, 3);
  });

  it('NO_RESPONSE → follow-up +2 hari', () => {
    const effect = resolveOutcomeEffect('NO_RESPONSE', 'act-1', new Date('2026-09-06T02:00:00Z'));
    assert.equal(effect.nextFollowUpDays, 2);
  });

  it('outcome undefined → efek netral', () => {
    assert.deepEqual(resolveOutcomeEffect(undefined, 'act-1', new Date()), { stage: null, followUp: null, nextFollowUpDays: null });
  });
});
```

- [ ] **Step 2: Jalankan → FAIL** (`pnpm --filter @promotor/platform-api test`)

- [ ] **Step 3: Implementasi domain**

Di `apps/platform-api/src/domain/next-action-rules.ts`:

```ts
export type ContactWaOutcome = 'INTERESTED_TEST' | 'ASK_SCHEDULE' | 'WAIT_PAYDAY' | 'NO_RESPONSE';

export interface OutcomeEffect {
  stage: 'INTERESTED' | null;
  followUp: { actionType: 'FOLLOW_UP' | 'MANUAL'; title: string; dueAt: Date; priority: number; idempotencyKey: string } | null;
  nextFollowUpDays: number | null;
}

export function resolveOutcomeEffect(outcome: ContactWaOutcome | undefined, actionId: string, now: Date): OutcomeEffect {
  const day = 24 * 3600_000;
  switch (outcome) {
    case 'INTERESTED_TEST':
      return {
        stage: 'INTERESTED',
        followUp: { actionType: 'FOLLOW_UP', title: 'Ajak booking Tes STIFIn', dueAt: new Date(now.getTime() + 2 * day), priority: 70, idempotencyKey: `wa-outcome:INTERESTED_TEST:${actionId}` },
        nextFollowUpDays: null,
      };
    case 'ASK_SCHEDULE':
      return {
        stage: null,
        followUp: { actionType: 'MANUAL', title: 'Kunci jadwal konsultasi', dueAt: new Date(now.getTime() + 1 * day), priority: 80, idempotencyKey: `wa-outcome:ASK_SCHEDULE:${actionId}` },
        nextFollowUpDays: null,
      };
    case 'WAIT_PAYDAY':
      return { stage: null, followUp: null, nextFollowUpDays: 3 };
    case 'NO_RESPONSE':
      return { stage: null, followUp: null, nextFollowUpDays: 2 };
    default:
      return { stage: null, followUp: null, nextFollowUpDays: null };
  }
}
```

- [ ] **Step 4: Contracts + endpoint**

Contracts (ganti body `ConfirmWhatsAppSentRequestSchema`):

```ts
export const ContactWaOutcomeSchema = z.enum(['INTERESTED_TEST', 'ASK_SCHEDULE', 'WAIT_PAYDAY', 'NO_RESPONSE']);
export type ContactWaOutcome = z.infer<typeof ContactWaOutcomeSchema>;

export const ConfirmWhatsAppSentRequestSchema = z.object({
  nextActionId: z.string().uuid('Valid nextActionId is required'),
  outcome: ContactWaOutcomeSchema.optional(),
  scheduleNextFollowUpDays: z.number().int().min(1).max(60).optional(),
});
export type ConfirmWhatsAppSentRequest = z.infer<typeof ConfirmWhatsAppSentRequestSchema>;
```

> Cek penggunaan `ConfirmWhatsAppSentRequest` di flow-web (`modules/messaging/commands.ts`) — lama hanya `{ nextActionId }`; schema baru backward-compatible.

`flow-routes.ts` `POST /messaging/confirm-sent` (baris ±581): setelah `nextActionService.completeAction(...)` sukses, jika `parsed.data.outcome`:

```ts
const effect = resolveOutcomeEffect(parsed.data.outcome, parsed.data.nextActionId, new Date());
if (effect.stage) {
  await contactLifecycleService.transitionStage(ctx, { contactId: action.contactId, stage: effect.stage, lostReason: null }, actor);
}
let createdAction = null;
if (effect.followUp) {
  createdAction = await nextActionRepo.create({
    organizationId: ctx.organizationId,
    contactId: action.contactId,
    actionType: effect.followUp.actionType,
    title: effect.followUp.title,
    description: null,
    dueAt: effect.followUp.dueAt,
    priority: effect.followUp.priority,
    status: 'PENDING',
    source: 'MANUAL',
    idempotencyKey: effect.followUp.idempotencyKey,
  });
}
return c.json({ ok: true, createdAction }, 200);
```

> Sesuaikan dengan bentuk aktual `nextActionRepo.create` & `completeAction` (baca file repo/service). Pastikan idempotency key unik → pemanggilan ulang tidak menduplikasi action.
> `WAIT_PAYDAY`/`NO_RESPONSE`: jika `scheduleNextFollowUpDays` tidak dikirim client, pakai `effect.nextFollowUpDays` sebagai nilai untuk logika schedule-follow-up yang sudah ada di `completeAction` (parameter `scheduleNextFollowUpDays` existing).

- [ ] **Step 5: UI — outcome picker di WhatsAppBottomSheet**

Perubahan `WhatsAppBottomSheet.tsx`:
1. Props: tambah `onConfirmSent: (scheduleNextDays?: number, outcome?: ContactWaOutcome) => Promise<void>`.
2. State baru: `const [outcome, setOutcome] = useState<ContactWaOutcome | undefined>(undefined);` reset di `useEffect` existing.
3. Setelah `hasOpenedWa === true`, di ATAS picker delay render picker outcome:

```tsx
<div style={{ marginTop: 14 }}>
  <div className="field-label">Apa hasil chat barusan?</div>
  <div className="segmented">
    {[
      { label: 'Tertarik Tes STIFIn', value: 'INTERESTED_TEST' as const },
      { label: 'Minta Jadwal', value: 'ASK_SCHEDULE' as const },
      { label: 'Tunggu Gajian', value: 'WAIT_PAYDAY' as const },
      { label: 'Tidak Merespons', value: 'NO_RESPONSE' as const },
    ].map((opt) => (
      <button key={opt.value} type="button"
        className={outcome === opt.value ? 'is-active' : undefined}
        aria-pressed={outcome === opt.value}
        onClick={() => setOutcome(opt.value)}>
        {opt.label}
      </button>
    ))}
  </div>
</div>
```

4. Picker delay lama: sembunyikan bila outcome ∈ {INTERESTED_TEST, ASK_SCHEDULE} (sistem yang menentukan), tampilkan bila `WAIT_PAYDAY`/`NO_RESPONSE` (bisa dioverride manual).
5. `handleConfirm`: `await onConfirmSent(outcome === 'INTERESTED_TEST' || outcome === 'ASK_SCHEDULE' ? undefined : nextFollowUpDays, outcome);`

Update semua pemanggil `onConfirmSent` (TodayPage, ContactDetailPage) dan `confirmWhatsAppSent` di `modules/messaging/commands.ts` agar meneruskan `outcome` + `scheduleNextFollowUpDays` ke body request.

- [ ] **Step 6: Test → PASS, typecheck, commit**

Run: `pnpm --filter @promotor/platform-api test && pnpm --filter @promotor/platform-api test:integration && pnpm --filter @promotor/promotor-flow-web test && pnpm typecheck`

```bash
git add -A
git commit -m "feat(flow): modal outcome pasca-chat WA dengan follow-up otomatis per hasil"
```

---

### Task C2: Quick Activity Notes (Catatan Cepat per Prospek)

**Files:**
- Modify: `apps/platform-api/src/routes/flow-routes.ts` (POST `/contacts/:id/notes`)
- Modify: `apps/platform-api/src/repositories/activity-repository.ts` (create note activity)
- Modify: `packages/contracts/src/index.ts` (`CreateContactNoteRequestSchema`)
- Modify: `apps/promotor-flow-web/src/app/(promotor)/app/contacts/[contactId]/page.tsx` (composer + list)
- Modify: `apps/promotor-flow-web/src/modules/contacts/commands.ts` (perluas)
- Test: `apps/platform-api/src/__tests__/integration/contact-note.integration.test.ts`

**Interfaces:**
- Consumes: tabel `activities` (`event_type`, `metadata_json`, `occurred_at`), tipe aktivitas `NOTE_ADDED` (sudah ada di fixtures).
- Produces: `POST /api/v1/flow/contacts/:id/notes` body `{ body: string }` (1..1000) → 201 `{ activity: { id, occurredAt } }`; aktivitas terbaca otomatis di timeline kontak existing (`GET /contacts/:id/activities`).

- [ ] **Step 1: Test integrasi yang gagal**

```ts
// apps/platform-api/src/__tests__/integration/contact-note.integration.test.ts
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { withIntegrationDb } from './test-env';

describe('POST /api/v1/flow/contacts/:id/notes', () => {
  it('membuat aktivitas NOTE_ADDED dan muncul di timeline', async () => {
    await withIntegrationDb(async (db) => {
      const { sessionToken, contactId } = await seedFlowContactFixture(db);
      const res = await requestOperator(db, 'POST', `/api/v1/flow/contacts/${contactId}/notes`, sessionToken,
        { body: 'Anak kelas 2 SMP, pemalu, suka melukis — cocok tes minat bakat.' });
      assert.equal(res.status, 201);

      const timeline = await requestOperator(db, 'GET', `/api/v1/flow/contacts/${contactId}/activities`, sessionToken);
      const note = timeline.body.activities.find((a: any) => a.eventType === 'NOTE_ADDED');
      assert.ok(note);
      assert.match(JSON.stringify(note.metadata), /melukis/);
    });
  });

  it('menolak body kosong', async () => {
    await withIntegrationDb(async (db) => {
      const { sessionToken, contactId } = await seedFlowContactFixture(db);
      const res = await requestOperator(db, 'POST', `/api/v1/flow/contacts/${contactId}/notes`, sessionToken, { body: '  ' });
      assert.equal(res.status, 400);
    });
  });
});
```

- [ ] **Step 2: Jalankan → FAIL**

- [ ] **Step 3: Contracts + endpoint + repo**

Contracts:

```ts
export const CreateContactNoteRequestSchema = z.object({
  body: z.string().trim().min(1, 'Catatan tidak boleh kosong').max(1000, 'Catatan maksimal 1000 karakter'),
});
export type CreateContactNoteRequest = z.infer<typeof CreateContactNoteRequestSchema>;
```

`flow-routes.ts` (dekat route activities):

```ts
app.post('/api/v1/flow/contacts/:id/notes', async (c) => {
  const parsed = CreateContactNoteRequestSchema.safeParse(await c.req.json().catch(() => ({})));
  if (!parsed.success) {
    throw new DomainError('VALIDATION_ERROR', parsed.error.issues.map((i) => i.message).join(', '));
  }
  const ctx = c.get('flowContext') as OrganizationContext; // sesuaikan nama context di file
  const activityRepo = c.get('activityRepo');
  const activity = await activityRepo.createNote({
    organizationId: ctx.organizationId,
    contactId: c.req.param('id'),
    body: parsed.data.body,
    occurredAt: new Date(),
  });
  return c.json({ activity }, 201);
});
```

`activity-repository.ts` — `createNote` insert `event_type='NOTE_ADDED'`, `metadata_json={ note: body }`, `occurred_at`. Pastikan `listByContact` mengembalikan metadata (cek mapping existing).

- [ ] **Step 4: UI composer di ContactDetailPage**

Di `apps/promotor-flow-web/src/app/(promotor)/app/contacts/[contactId]/page.tsx`, di atas timeline aktivitas:

```tsx
<section style={{ marginTop: 12 }}>
  <div className="field-label">Catatan Cepat</div>
  <textarea className="textarea" rows={2} value={quickNote} onChange={(e) => setQuickNote(e.target.value)}
    placeholder="mis. Anak kelas 2 SMP, pemalu, suka melukis..." aria-label="Catatan cepat" />
  <div style={{ display: 'flex', gap: 8, marginTop: 8 }}>
    <button type="button" className="btn btn-primary btn-sm" disabled={quickNote.trim().length === 0 || savingNote}
      onClick={handleSaveNote}>{savingNote ? 'Menyimpan...' : 'Simpan Catatan'}</button>
  </div>
</section>
```

handler memanggil command baru `addContactNoteCommand(contactId, body)` (pola `lifecycleCommands` di `modules/lifecycle/commands.ts` — tambahkan di module contacts), lalu `useToast` "Catatan tersimpan ✓" + reload activities.

- [ ] **Step 5: Test → PASS, typecheck, commit**

Run: `pnpm --filter @promotor/platform-api test:integration && pnpm --filter @promotor/promotor-flow-web test && pnpm typecheck`

```bash
git add -A
git commit -m "feat(flow): catatan cepat timestamped per prospek di contact detail"
```

---

### Task C3: Tambah ke Kalender (Google Calendar + .ics)

**Files:**
- Modify: `packages/platform-core/src/index.ts` (2 util pure)
- Create: `packages/platform-core/src/__tests__/calendar.test.ts` (+ pastikan script `test` ada di `packages/platform-core/package.json`; bila belum, tambah `"test": "tsx --test src/__tests__/*.test.ts"`)
- Modify: `apps/promotor-flow-web/src/app/(promotor)/app/contacts/[contactId]/page.tsx` (sheet booking) + `src/app/(promotor)/app/calendar/page.tsx`
- Create: `apps/promotor-flow-web/src/components/calendar/CalendarButtons.tsx`

**Interfaces:**
- Consumes: objek booking `{ serviceTitle, startAt (ISO), endAt? (ISO), locationText? }`.
- Produces:
  - `buildGoogleCalendarUrl(ev: CalendarEvent): string` (menghasilkan `https://calendar.google.com/calendar/render?action=TEMPLATE&text=...&dates=YYYYMMDDTHHmmssZ/...&details=...&location=...`).
  - `buildIcsContent(ev: CalendarEvent): string` (VCALENDAR 2.0, UID deterministik `ralivo-{startAt}-{title slug}@ralivo`, DTSTAMP, DTSTART/DTEND UTC, SUMMARY, LOCATION, DESCRIPTION, `METHOD:PUBLISH`).
  - `<CalendarButtons event />` → tombol "Google Calendar" (window.open) + "Unduh .ics" (Blob `text/calendar`).
  - `interface CalendarEvent { title: string; startAt: string; endAt?: string | null; details?: string | null; location?: string | null; }` (default durasi 60 menit bila endAt kosong).

- [ ] **Step 1: Test util yang gagal**

```ts
// packages/platform-core/src/__tests__/calendar.test.ts
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { buildGoogleCalendarUrl, buildIcsContent } from '../index';

const ev = {
  title: 'Sesi Konsultasi STIFIn — Ayu',
  startAt: '2026-09-10T02:00:00.000Z', // 09:00 WIB
  endAt: '2026-09-10T03:00:00.000Z',
  details: 'Dibuat otomatis dari Ralivo Flow',
  location: 'Online via Zoom',
};

describe('calendar utils', () => {
  it('google url memuat template, dates UTC, dan title ter-encode', () => {
    const url = buildGoogleCalendarUrl(ev);
    assert.ok(url.startsWith('https://calendar.google.com/calendar/render?action=TEMPLATE'));
    assert.ok(url.includes('dates=20260910T020000Z%2F20260910T030000Z') || url.includes('dates=20260910T020000Z/20260910T030000Z'));
    assert.ok(decodeURIComponent(url).includes('Sesi Konsultasi STIFIn — Ayu'));
  });

  it('ics berisi VCALENDAR, UTC timestamps, dan escape koma', () => {
    const ics = buildIcsContent({ ...ev, location: 'Jakarta, Indonesia' });
    assert.ok(ics.startsWith('BEGIN:VCALENDAR'));
    assert.ok(ics.includes('BEGIN:VEVENT'));
    assert.ok(ics.includes('DTSTART:20260910T020000Z'));
    assert.ok(ics.includes('DTEND:20260910T030000Z'));
    assert.ok(ics.includes('LOCATION:Jakarta\\, Indonesia'));
    assert.ok(ics.endsWith('END:VCALENDAR'));
  });

  it('durasi default 60 menit bila endAt kosong', () => {
    const ics = buildIcsContent({ title: 'T', startAt: '2026-09-10T02:00:00.000Z', endAt: null });
    assert.ok(ics.includes('DTEND:20260910T030000Z'));
  });
});
```

- [ ] **Step 2: Jalankan → FAIL** (`pnpm --filter @promotor/platform-core test`)

- [ ] **Step 3: Implementasi**

```ts
// packages/platform-core/src/index.ts (tambah di akhir)
export interface CalendarEvent {
  title: string;
  startAt: string;
  endAt?: string | null;
  details?: string | null;
  location?: string | null;
}

function toGoogleStamp(iso: string): string {
  return new Date(iso).toISOString().replace(/[-:]/g, '').replace(/\.\d{3}/, '');
}

function toIcsStamp(iso: string): string {
  return new Date(iso).toISOString().replace(/[-:]/g, '').replace(/\.\d{3}/, '');
}

function escapeIcs(text: string): string {
  return text.replace(/\\/g, '\\\\').replace(/;/g, '\\;').replace(/,/g, '\\,').replace(/\n/g, '\\n');
}

export function buildGoogleCalendarUrl(ev: CalendarEvent): string {
  const end = ev.endAt ?? new Date(new Date(ev.startAt).getTime() + 60 * 60_000).toISOString();
  const params = new URLSearchParams({
    action: 'TEMPLATE',
    text: ev.title,
    dates: `${toGoogleStamp(ev.startAt)}/${toGoogleStamp(end)}`,
  });
  if (ev.details) params.set('details', ev.details);
  if (ev.location) params.set('location', ev.location);
  return `https://calendar.google.com/calendar/render?${params.toString()}`;
}

export function buildIcsContent(ev: CalendarEvent): string {
  const end = ev.endAt ?? new Date(new Date(ev.startAt).getTime() + 60 * 60_000).toISOString();
  const slug = ev.title.toLowerCase().replace(/[^a-z0-9]+/g, '-').slice(0, 40);
  const lines = [
    'BEGIN:VCALENDAR',
    'VERSION:2.0',
    'PRODID:-//Ralivo Flow//Calendar Export//ID',
    'METHOD:PUBLISH',
    'BEGIN:VEVENT',
    `UID:ralivo-${toIcsStamp(ev.startAt)}-${slug}@ralivo`,
    `DTSTAMP:${toIcsStamp(new Date().toISOString())}`,
    `DTSTART:${toIcsStamp(ev.startAt)}`,
    `DTEND:${toIcsStamp(end)}`,
    `SUMMARY:${escapeIcs(ev.title)}`,
    ev.details ? `DESCRIPTION:${escapeIcs(ev.details)}` : null,
    ev.location ? `LOCATION:${escapeIcs(ev.location)}` : null,
    'END:VEVENT',
    'END:VCALENDAR',
  ].filter((l): l is string => l !== null);
  return lines.join('\r\n');
}
```

- [ ] **Step 4: Test → PASS** → **Step 5: Komponen + integrasi UI**

```tsx
// apps/promotor-flow-web/src/components/calendar/CalendarButtons.tsx
'use client';

import React from 'react';
import { buildGoogleCalendarUrl, buildIcsContent, CalendarEvent } from '@promotor/platform-core';

export function CalendarButtons({ event }: { event: CalendarEvent }) {
  const downloadIcs = () => {
    const blob = new Blob([buildIcsContent(event)], { type: 'text/calendar;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url; a.download = 'ralivo-jadwal.ics';
    a.click();
    URL.revokeObjectURL(url);
  };
  return (
    <div style={{ display: 'flex', gap: 8, marginTop: 12 }}>
      <button type="button" className="btn btn-secondary btn-sm" onClick={() => window.open(buildGoogleCalendarUrl(event), '_blank')}>
        + Google Calendar
      </button>
      <button type="button" className="btn btn-secondary btn-sm" onClick={downloadIcs}>Unduh .ics</button>
    </div>
  );
}
```

Pasang `<CalendarButtons event={{ title: \`${serviceTitle} — ${contactName}\`, startAt: booking.startAt, endAt: booking.endAt, details: 'Jadwal dari Ralivo Flow', location: booking.locationText ?? undefined }} />` pada: (1) sheet konfirmasi booking di ContactDetailPage setelah booking dibuat/confirmed, (2) daftar booking di CalendarPage per item hari terpilih.

- [ ] **Step 6: Typecheck + commit**

Run: `pnpm typecheck && pnpm --filter @promotor/platform-core test`

```bash
git add -A
git commit -m "feat(flow): tombol tambah-ke-kalender (Google Calendar + .ics) untuk jadwal konsultasi"
```

---

### Task C4: Persona Template WhatsApp (Formal / Hangat / Urgent)

**Files:**
- Modify: `apps/platform-api/src/db/schema/message-templates.ts` (kolom `tone`)
- Modify: `packages/contracts/src/index.ts` (`MessageTemplateToneSchema`, field pada template schemas)
- Modify: `apps/platform-api/src/services/template-service.ts` (create/update tone; helper pick by tone)
- Modify: `apps/promotor-flow-web/src/modules/messaging/queries.ts` (`generateDraftMessage` + tone), `modules/messaging/ports.ts` & adapters http/mock (ikut pola)
- Modify: `apps/promotor-flow-web/src/components/today/WhatsAppBottomSheet.tsx` (segmented tone) + parent (`TodayPage`, `ContactDetailPage`)
- Modify: `apps/promotor-flow-web/src/app/(promotor)/app/templates/page.tsx` (field tone)
- Modify: `packages/promotor-flow-fixtures/src/index.ts` (seed variasi tone)
- Test: `apps/promotor-flow-web/src/__tests__/template-tone.test.ts`

**Interfaces:**
- Produces:
  - `MessageTemplateTone = 'FORMAL' | 'HANGAT' | 'URGENT'` (kolom nullable — null = netral/legacy).
  - `generateDraftMessage(category, contactName, context, tone?)` — pilih template `tone === tone` lebih dulu; fallback ke template `tone === null`; terakhir fallback ke template pertama kategori.
  - Sheet: segmented "Formal | Hangat | Urgent" yang meregenerasi draft saat diganti (via callback `onRegenerate(tone)` dari parent — draft dibuat ulang dari template).

- [ ] **Step 1: Test pemilihan tone yang gagal**

```ts
// apps/promotor-flow-web/src/__tests__/template-tone.test.ts
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { pickTemplateByTone } from '../modules/messaging/queries';

const templates = [
  { id: '1', category: 'FOLLOW_UP', templateText: 'Netral [Nama]', tone: null, title: 'T1', isActive: true },
  { id: '2', category: 'FOLLOW_UP', templateText: 'Formal [Nama]', tone: 'FORMAL', title: 'T2', isActive: true },
  { id: '3', category: 'FOLLOW_UP', templateText: 'Hangat [Nama]', tone: 'HANGAT', title: 'T3', isActive: true },
];

describe('pickTemplateByTone', () => {
  it('memilih template sesuai tone', () => {
    assert.equal(pickTemplateByTone(templates as any, 'FOLLOW_UP', 'HANGAT')?.id, '3');
  });
  it('fallback ke netral bila tone tidak tersedia', () => {
    assert.equal(pickTemplateByTone(templates as any, 'FOLLOW_UP', 'URGENT')?.id, '1');
  });
  it('fallback ke template pertama bila tidak ada netral', () => {
    const noNeutral = templates.filter((t) => t.tone !== null);
    assert.equal(pickTemplateByTone(noNeutral as any, 'FOLLOW_UP', 'URGENT')?.id, '2');
  });
});
```

- [ ] **Step 2: Jalankan → FAIL**

- [ ] **Step 3: Migrasi + contracts + service + query**

Schema `message-templates.ts`: `tone: varchar('tone', { length: 16 })` (nullable). Generate + migrate.

Contracts:

```ts
export const MessageTemplateToneSchema = z.enum(['FORMAL', 'HANGAT', 'URGENT']);
export type MessageTemplateTone = z.infer<typeof MessageTemplateToneSchema>;
// MessageTemplateSchema + tone: MessageTemplateToneSchema.nullable().optional()
// CreateMessageTemplateRequestSchema / Update (bila ada) + tone optional
```

`modules/messaging/queries.ts`:

```ts
export function pickTemplateByTone(templates: MessageTemplate[], category: string, tone?: MessageTemplateTone | null): MessageTemplate | undefined {
  const inCategory = templates.filter((t) => t.category === category && t.isActive !== false);
  if (tone) {
    const exact = inCategory.find((t) => t.tone === tone);
    if (exact) return exact;
  }
  return inCategory.find((t) => t.tone == null) ?? inCategory[0];
}

export function generateDraftMessage(
  category: string, contactName: string, context?: DraftContext, tone?: MessageTemplateTone | null
): string {
  const template = pickTemplateByTone(getTemplates() /* sesuai sumber data module */, category, tone);
  if (!template) return '';
  // interpolasi existing [Nama], [Tanggal], [Amount], [Layanan] — biarkan persis seperti sekarang
}
```

- [ ] **Step 4: UI tone di sheet + templates page**

`WhatsAppBottomSheet.tsx`: props tambah `onRegenerateDraft?: (tone: MessageTemplateTone) => string` dan `initialTone?: MessageTemplateTone`. Segmented di atas textarea:

```tsx
<div className="segmented" style={{ marginTop: 8 }} role="group" aria-label="Nada pesan">
  {(['FORMAL', 'HANGAT', 'URGENT'] as const).map((t) => (
    <button key={t} type="button" className={tone === t ? 'is-active' : undefined} aria-pressed={tone === t}
      onClick={() => { setTone(t); const next = onRegenerateDraft?.(t); if (next !== undefined) setDraft(next); }}>
      {t === 'FORMAL' ? 'Formal' : t === 'HANGAT' ? 'Hangat' : 'Urgent'}
    </button>
  ))}
</div>
```

Parent (`TodayPage`, `ContactDetailPage`) mengisi `onRegenerateDraft={(t) => messagingQueries.generateDraftMessage(category, contact.name, context, t)}`. Templates page: `<select className="input">` tiga tone + opsi "Netral (kosong)".

Fixtures: tambah 2 template FOLLOW_UP baru dengan tone `HANGAT` dan `URGENT` di `SEED_TEMPLATES` (teks contoh Indonesia) agar mock mode bisa mendemokan.

- [ ] **Step 5: Test → PASS, typecheck, commit**

Run: `pnpm --filter @promotor/promotor-flow-web test && pnpm typecheck`

```bash
git add -A
git commit -m "feat(flow): persona template WA (formal/hangat/urgent) dengan pemilihan cepat di sheet"
```

---

### Task C5: Filter Cerdas di Menu Kontak

**Files:**
- Modify: `apps/platform-api/src/services/contact-flow-service.ts` (`listContacts` params: `stage`, `neverContacted`, `followUpOverdue`)
- Modify: `apps/platform-api/src/routes/flow-routes.ts:94` (parse query)
- Modify: `packages/contracts/src/index.ts` (`ListFlowContactsQuerySchema`)
- Modify: `apps/promotor-flow-web/src/app/(promotor)/app/contacts/page.tsx` (chips filter)
- Modify: `apps/promotor-flow-web/src/modules/contacts/queries.ts` (perluas params)
- Test: `apps/platform-api/src/__tests__/integration/contact-filters.integration.test.ts`

**Interfaces:**
- Produces: query params `stage` (enum lifecycle), `neverContacted=true` (tidak ada aktivitas `WHATSAPP_SENT`/`WA_SENT`), `followUpOverdue=true` (ada `next_actions` PENDING dengan `due_at < now`). Chips UI: Semua | Belum Dihubungi | Follow-up Terlambat | Prospek | Klien.

- [ ] **Step 1: Test integrasi yang gagal**

```ts
// apps/platform-api/src/__tests__/integration/contact-filters.integration.test.ts
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { withIntegrationDb } from './test-env';

describe('filter cerdas kontak', () => {
  it('neverContacted hanya kontak tanpa aktivitas WA', async () => {
    await withIntegrationDb(async (db) => {
      const { sessionToken, contactedId, untouchedId } = await seedTwoContactsOneContacted(db);
      const res = await requestOperator(db, 'GET', '/api/v1/flow/contacts?neverContacted=true', sessionToken);
      const ids = res.body.contacts.map((c: any) => c.id);
      assert.ok(ids.includes(untouchedId));
      assert.ok(!ids.includes(contactedId));
    });
  });

  it('followUpOverdue hanya kontak dengan action PENDING lewat jatuh tempo', async () => {
    await withIntegrationDb(async (db) => {
      const { sessionToken, overdueContactId, futureContactId } = await seedOverdueAndFutureContacts(db);
      const res = await requestOperator(db, 'GET', '/api/v1/flow/contacts?followUpOverdue=true', sessionToken);
      const ids = res.body.contacts.map((c: any) => c.id);
      assert.ok(ids.includes(overdueContactId));
      assert.ok(!ids.includes(futureContactId));
    });
  });
});
```

- [ ] **Step 2: Jalankan → FAIL**

- [ ] **Step 3: Implementasi service + route**

`contact-flow-service.ts` `listContacts` — tambah param & kondisi:

```ts
if (query.stage) baseConditions.push(eq(contactFlowStates.stage, query.stage)); // join contact_flow_states existing — cek bagaimana stage diambil saat ini

if (query.neverContacted) {
  baseConditions.push(sql`NOT EXISTS (
    SELECT 1 FROM activities a
    WHERE a.contact_id = ${contacts.id}
      AND a.event_type IN ('WHATSAPP_SENT', 'WA_SENT')
  )`);
}
if (query.followUpOverdue) {
  baseConditions.push(sql`EXISTS (
    SELECT 1 FROM next_actions na
    WHERE na.contact_id = ${contacts.id}
      AND na.status = 'PENDING'
      AND na.due_at < now()
  )`);
}
```

> Cek event_type persis yang dipakai `buildWaDeepLink` (baca `messaging-service.ts`) dan status enum `next_actions`.

`flow-routes.ts` GET contacts: parse & validasi params (zod), teruskan. Contracts:

```ts
export const ListFlowContactsQuerySchema = z.object({
  search: z.string().max(120).optional(),
  classification: z.enum(['PROSPECT', 'CLIENT']).optional(),
  stage: ContactLifecycleStageSchema.optional(),
  neverContacted: z.coerce.boolean().optional(),
  followUpOverdue: z.coerce.boolean().optional(),
  limit: z.coerce.number().int().min(1).max(100).default(50),
  offset: z.coerce.number().int().min(0).default(0),
});
```

- [ ] **Step 4: UI chips di contacts page**

Di `ContactsPage` (sekarang hanya `SegmentedControl` ALL/PROSPECT/CLIENT):

```tsx
<div style={{ display: 'flex', gap: 8, overflowX: 'auto', padding: '8px 12px' }} role="group" aria-label="Filter cepat">
  {[
    { label: 'Semua', params: '' },
    { label: 'Belum Dihubungi', params: 'neverContacted=true' },
    { label: 'Follow-up Terlambat', params: 'followUpOverdue=true' },
    { label: 'Prospek', params: 'classification=PROSPECT' },
    { label: 'Klien', params: 'classification=CLIENT' },
  ].map((f) => (
    <button key={f.label} type="button" className={`btn btn-sm ${activeFilter === f.params ? 'btn-primary' : 'btn-secondary'}`}
      aria-pressed={activeFilter === f.params}
      onClick={() => { setActiveFilter(f.params); reloadContacts(f.params); }}>
      {f.label}
    </button>
  ))}
</div>
```

`reloadContacts(queryString)` memanggil query module dengan param tambahan (perluas `modules/contacts/queries.ts` untuk menerima query string/objek).

- [ ] **Step 5: Test → PASS, typecheck, commit**

Run: `pnpm --filter @promotor/platform-api test:integration && pnpm typecheck`

```bash
git add -A
git commit -m "feat(flow): filter cerdas kontak (belum dihubungi, follow-up terlambat, prospek/klien)"
```

---

### Task C6: Visual Pipeline (Kanban Board)

**Files:**
- Create: `apps/promotor-flow-web/src/modules/pipeline/grouping.ts`
- Create: `apps/promotor-flow-web/src/app/(promotor)/app/pipeline/page.tsx`
- Modify: `apps/promotor-flow-web/src/app/(promotor)/app/more/page.tsx` (link "Pipeline")
- Test: `apps/promotor-flow-web/src/__tests__/pipeline-grouping.test.ts`

**Interfaces:**
- Consumes: `contactQueries.listContacts()` (limit 100 — cukup untuk MVP), `lifecycleCommands.changeStage(contactId, stage)`, stage enum existing (`NEW, CONTACTED, INTERESTED, FOLLOW_UP, BOOKED, COMPLETED, LOST`).
- Produces: `groupContactsByStage(contacts): Record<ContactLifecycleStage, FlowContact[]>` + halaman `/app/pipeline` kolom horizontal scroll; pindah stage via **drag HTML5** (desktop) dan **menu "Pindah tahap"** di kartu (mobile).

- [ ] **Step 1: Test grouping yang gagal**

```ts
// apps/promotor-flow-web/src/__tests__/pipeline-grouping.test.ts
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { PIPELINE_STAGES, groupContactsByStage } from '../modules/pipeline/grouping';

describe('groupContactsByStage', () => {
  it('mengelompokkan sesuai stage dan menyertakan kolom kosong', () => {
    const contacts = [
      { id: '1', name: 'Ayu', stage: 'NEW' },
      { id: '2', name: 'Arief', stage: 'BOOKED' },
      { id: '3', name: 'Reni', stage: 'BOOKED' },
    ] as any[];
    const grouped = groupContactsByStage(contacts);
    assert.deepEqual(PIPELINE_STAGES, ['NEW', 'CONTACTED', 'INTERESTED', 'FOLLOW_UP', 'BOOKED', 'COMPLETED', 'LOST']);
    assert.equal(grouped.NEW.length, 1);
    assert.equal(grouped.BOOKED.length, 2);
    assert.equal(grouped.LOST.length, 0);
  });
});
```

- [ ] **Step 2: Jalankan → FAIL** → **Step 3: Implementasi grouping**

```ts
// apps/promotor-flow-web/src/modules/pipeline/grouping.ts
import { ContactLifecycleStage } from '@promotor/contracts';

export const PIPELINE_STAGES: ContactLifecycleStage[] =
  ['NEW', 'CONTACTED', 'INTERESTED', 'FOLLOW_UP', 'BOOKED', 'COMPLETED', 'LOST'];

export const STAGE_LABELS: Record<ContactLifecycleStage, string> = {
  NEW: 'Lead Baru',
  CONTACTED: 'Terhubung',
  INTERESTED: 'Tertarik',
  FOLLOW_UP: 'Follow-up',
  BOOKED: 'Jadwal Dibuat',
  COMPLETED: 'Tes Selesai',
  LOST: 'Lost',
};

export function groupContactsByStage<T extends { stage: ContactLifecycleStage }>(contacts: T[]): Record<ContactLifecycleStage, T[]> {
  const grouped = Object.fromEntries(PIPELINE_STAGES.map((s) => [s, []])) as Record<ContactLifecycleStage, T[]>;
  for (const c of contacts) {
    if (grouped[c.stage]) grouped[c.stage].push(c);
  }
  return grouped;
}
```

- [ ] **Step 4: Halaman Kanban**

```tsx
// apps/promotor-flow-web/src/app/(promotor)/app/pipeline/page.tsx
'use client';

import React, { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { AppShell, PageHeader, LoadingRows, ErrorState } from '@/components/ui'; // sesuaikan import shell flow-web
import { contactQueries } from '@/modules/contacts/queries';
import { lifecycleCommands } from '@/modules/lifecycle/commands';
import { PIPELINE_STAGES, STAGE_LABELS, groupContactsByStage } from '@/modules/pipeline/grouping';
import { formatPhoneDisplay } from '@promotor/platform-core';
import { ContactLifecycleStage } from '@promotor/contracts';

export default function PipelinePage() {
  const router = useRouter();
  const [contacts, setContacts] = useState<any[] | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [menuContact, setMenuContact] = useState<{ id: string; name: string } | null>(null);

  const load = () => contactQueries.listContacts({ limit: 100 }).then((res: any) => setContacts(res.contacts ?? res)).catch(() => setError('Gagal memuat pipeline.'));
  useEffect(() => { load(); }, []);

  if (error) return <ErrorState message={error} />;
  if (!contacts) return <LoadingRows rows={6} />;
  const grouped = groupContactsByStage(contacts as any[]);

  const moveTo = async (contactId: string, stage: ContactLifecycleStage) => {
    await lifecycleCommands.changeStage(contactId, stage, stage === 'LOST' ? 'Tidak sesuai' : undefined);
    await load();
  };

  return (
    <AppShell>
      <PageHeader title="Pipeline" subtitle={`${contacts.length} kontak · geser kartu antar tahap`} />
      <div style={{ display: 'flex', gap: 12, overflowX: 'auto', padding: 12, alignItems: 'flex-start' }}>
        {PIPELINE_STAGES.map((stage) => (
          <section key={stage}
            style={{ minWidth: 240, flex: '0 0 240px', background: 'var(--canvas, #f8fafc)', border: '1px solid var(--border)', padding: 8 }}
            onDragOver={(e) => e.preventDefault()}
            onDrop={(e) => {
              const contactId = e.dataTransfer.getData('text/contact-id');
              if (contactId) void moveTo(contactId, stage);
            }}>
            <div className="kicker">{STAGE_LABELS[stage]} · {grouped[stage].length}</div>
            {grouped[stage].map((c) => (
              <div key={c.id} draggable
                onDragStart={(e) => e.dataTransfer.setData('text/contact-id', c.id)}
                style={{ background: '#fff', border: '1px solid var(--border)', padding: 10, marginTop: 8, cursor: 'grab' }}
                onClick={() => router.push(`/app/contacts/${c.id}`)}>
                <strong style={{ font: '600 13px/1.3 var(--font-sans)' }}>{c.name}</strong>
                <div className="kicker kicker-muted">{formatPhoneDisplay(c.phoneE164)}</div>
                <button type="button" className="btn btn-ghost btn-sm" style={{ marginTop: 6 }}
                  onClick={(e) => { e.stopPropagation(); setMenuContact({ id: c.id, name: c.name }); }}>
                  Pindah tahap
                </button>
              </div>
            ))}
            {grouped[stage].length === 0 && (
              <div className="kicker kicker-muted" style={{ marginTop: 8 }}>Kosong</div>
            )}
          </section>
        ))}
      </div>

      {menuContact && (
        <div role="dialog" aria-label={`Pindah tahap ${menuContact.name}`}
          style={{ position: 'fixed', inset: 0, background: 'rgba(15,23,42,.4)', display: 'flex', alignItems: 'flex-end', zIndex: 40 }}
          onClick={() => setMenuContact(null)}>
          <div style={{ background: '#fff', width: '100%', padding: 16 }} onClick={(e) => e.stopPropagation()}>
            <div className="field-label">Pindah {menuContact.name} ke tahap:</div>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8, marginTop: 8 }}>
              {PIPELINE_STAGES.map((s) => (
                <button key={s} type="button" className="btn btn-secondary btn-sm"
                  onClick={async () => { await moveTo(menuContact.id, s); setMenuContact(null); }}>
                  {STAGE_LABELS[s]}
                </button>
              ))}
            </div>
          </div>
        </div>
      )}
    </AppShell>
  );
}
```

> Sesuaikan import shell/header dengan komponen aktual flow-web (`AppShell`, `PageHeader` ada di `src/components/ui` atau `components/layout` — lihat pemakaian di TodayPage). Stage `LOST` via menu memakai lostReason default `'Tidak sesuai'` (aturan domain: LOST wajib reason).
>
> Link menu: tambahkan item "Pipeline" di `more/page.tsx` mengarah `/app/pipeline` (pola item existing).

- [ ] **Step 5: Test → PASS, typecheck, commit**

Run: `pnpm --filter @promotor/promotor-flow-web test && pnpm typecheck`

```bash
git add -A
git commit -m "feat(flow): kanban pipeline per tahap lifecycle dengan drag-drop dan menu pindah tahap"
```

---

### Task C7: Kalkulator Omset & Komisi di Header

**Files:**
- Modify: `apps/platform-api/src/db/schema/bookings.ts` (kolom `paid_at`)
- Modify: `apps/platform-api/src/services/booking-service.ts` (`markPaid` set `paidAt`)
- Create: `apps/platform-api/src/domain/revenue-summary.ts`
- Modify: `apps/platform-api/src/routes/flow-routes.ts` (GET `/revenue-summary`, GET/PUT `/revenue-settings`)
- Modify: `packages/contracts/src/index.ts` (schemas)
- Modify: `apps/promotor-flow-web/src/app/(promotor)/app/page.tsx` (header card) + `settings/page.tsx` (input komisi)
- Test: `apps/platform-api/src/__tests__/revenue-summary.test.ts`, `apps/platform-api/src/__tests__/integration/revenue-summary.integration.test.ts`

**Interfaces:**
- Produces:
  - `RevenueSummary = { period: 'WEEK' | 'MONTH'; paidCount: number; grossAmount: number; commissionPercent: number; estimatedCommission: number }`.
  - `GET /api/v1/flow/revenue-summary?period=MONTH|WEEK` — agregat bookings `paymentStatus='PAID'` dengan `paidAt` dalam periode (WIB, Senin-minggu untuk WEEK; bulan kalender untuk MONTH).
  - `GET /api/v1/flow/revenue-settings` → `{ commissionPercent: number }`; `PUT` body `{ commissionPercent: 0..100 }` — disimpan di `organizations.metadata` (`jsonb`, key `flowCommissionPercent`).
  - `computeRevenueSummary(bookings, { period, now, commissionPercent }): RevenueSummary` (pure, di domain).

- [ ] **Step 1: Unit test domain yang gagal**

```ts
// apps/platform-api/src/__tests__/revenue-summary.test.ts
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { computeRevenueSummary } from '../domain/revenue-summary';

describe('computeRevenueSummary', () => {
  const bookings = [
    { paymentStatus: 'PAID', paidAt: '2026-09-02T04:00:00.000Z', amount: 600000 },  // Sep, WIB 11:00
    { paymentStatus: 'PAID', paidAt: '2026-08-20T04:00:00.000Z', amount: 350000 },  // Agustus
    { paymentStatus: 'UNPAID', paidAt: '2026-09-03T04:00:00.000Z', amount: 999999 },
    { paymentStatus: 'PAID', paidAt: '2026-09-03T04:00:00.000Z', amount: 1200000 },
  ];

  it('bulan kalender (WIB) hanya memasukkan PAID bulan ini', () => {
    const res = computeRevenueSummary(bookings as any[], { period: 'MONTH', now: new Date('2026-09-06T00:00:00.000Z'), commissionPercent: 10 });
    assert.equal(res.paidCount, 2);
    assert.equal(res.grossAmount, 600000 + 1200000);
    assert.equal(res.estimatedCommission, 180000);
  });

  it('minggu berjalan (Senin sebagai awal, WIB)', () => {
    // 2026-09-06 adalah Minggu WIB → minggu mulai Senin 2026-08-31 WIB; kedua PAID Sept masih masuk
    const res = computeRevenueSummary(bookings as any[], { period: 'WEEK', now: new Date('2026-09-06T00:00:00.000Z'), commissionPercent: 0 });
    assert.equal(res.paidCount, 2);
  });

  it('komisi 0 → estimatedCommission 0', () => {
    const res = computeRevenueSummary([], { period: 'MONTH', now: new Date(), commissionPercent: 0 });
    assert.equal(res.estimatedCommission, 0);
  });
});
```

- [ ] **Step 2: Jalankan → FAIL**

- [ ] **Step 3: Implementasi domain + migrasi + service + endpoint**

Domain:

```ts
// apps/platform-api/src/domain/revenue-summary.ts
export interface RevenueSummary {
  period: 'WEEK' | 'MONTH';
  paidCount: number;
  grossAmount: number;
  commissionPercent: number;
  estimatedCommission: number;
}

// Batas periode dihitung di Asia/Jakarta (UTC+7) sesuai DEFAULT_ORGANIZATION_TIMEZONE
function wibOffsetMs(): number { return 7 * 3600_000; }

function startOfMonthWib(now: Date): Date {
  const wib = new Date(now.getTime() + wibOffsetMs());
  const start = Date.UTC(wib.getUTCFullYear(), wib.getUTCMonth(), 1, 0, 0, 0);
  return new Date(start - wibOffsetMs());
}

function startOfWeekWib(now: Date): Date {
  const wib = new Date(now.getTime() + wibOffsetMs());
  const dayOfWeek = wib.getUTCDay(); // 0=Min
  const daysSinceMonday = (dayOfWeek + 6) % 7;
  const start = Date.UTC(wib.getUTCFullYear(), wib.getUTCMonth(), wib.getUTCDate() - daysSinceMonday, 0, 0, 0);
  return new Date(start - wibOffsetMs());
}

export function computeRevenueSummary(
  bookings: Array<{ paymentStatus: string; paidAt: string | null; amount: number }>,
  input: { period: 'WEEK' | 'MONTH'; now: Date; commissionPercent: number }
): RevenueSummary {
  const from = input.period === 'MONTH' ? startOfMonthWib(input.now) : startOfWeekWib(input.now);
  let paidCount = 0;
  let grossAmount = 0;
  for (const b of bookings) {
    if (b.paymentStatus !== 'PAID' || !b.paidAt) continue;
    if (new Date(b.paidAt).getTime() < from.getTime()) continue;
    paidCount += 1;
    grossAmount += b.amount;
  }
  const commissionPercent = Math.max(0, Math.min(100, input.commissionPercent));
  return {
    period: input.period,
    paidCount,
    grossAmount,
    commissionPercent,
    estimatedCommission: Math.floor((grossAmount * commissionPercent) / 100),
  };
}
```

Migrasi: `bookings.ts` tambah `paidAt: timestamp('paid_at', { withTimezone: true })` → generate + migrate. `booking-service.ts` `markPaid`: set `paidAt: new Date()` (dan backfill tidak diperlukan untuk MVP).

Endpoints (`flow-routes.ts`):

```ts
app.get('/api/v1/flow/revenue-summary', async (c) => {
  const ctx = /* flow context */;
  const period = c.req.query('period') === 'WEEK' ? 'WEEK' : 'MONTH';
  const settings = await revenueSettingsService.get(ctx.organizationId); // baca organizations.metadata
  const bookings = await bookingRepo.listPaidWithPaidAt(ctx.organizationId);
  const summary = computeRevenueSummary(bookings, { period, now: new Date(), commissionPercent: settings.commissionPercent });
  return c.json({ summary }, 200);
});

app.get('/api/v1/flow/revenue-settings', /* read organizations.metadata.flowCommissionPercent ?? 0 */);
app.put('/api/v1/flow/revenue-settings', /* validate { commissionPercent: 0..100 } → merge metadata → 200 */);
```

Contracts:

```ts
export const RevenueSummarySchema = z.object({
  period: z.enum(['WEEK', 'MONTH']),
  paidCount: z.number().int().nonnegative(),
  grossAmount: z.number().int().nonnegative(),
  commissionPercent: z.number().int().min(0).max(100),
  estimatedCommission: z.number().int().nonnegative(),
});
export type RevenueSummary = z.infer<typeof RevenueSummarySchema>;

export const UpdateRevenueSettingsRequestSchema = z.object({
  commissionPercent: z.number().int().min(0, 'Persen komisi 0–100').max(100, 'Persen komisi 0–100'),
});
```

- [ ] **Step 4: UI header card + settings**

TodayPage — card di bawah `PageHeader`:

```tsx
const [summary, setSummary] = useState<RevenueSummary | null>(null);
useEffect(() => { /* GET revenue-summary?period=MONTH */ }, []);

{summary && (
  <section style={{ margin: '12px 12px 0', padding: 14, border: '1px solid var(--accent)', background: '#eff6ff' }}>
    <div className="kicker">Estimasi omzet bulan ini</div>
    <div style={{ font: '700 22px/1.2 var(--font-sans)' }}>
      Rp {summary.grossAmount.toLocaleString('id-ID')}
    </div>
    <div className="kicker kicker-muted">
      {summary.paidCount} tes ter-closing{summary.commissionPercent > 0 ? ` · estimasi komisi ${summary.commissionPercent}% = Rp ${summary.estimatedCommission.toLocaleString('id-ID')}` : ''}
    </div>
  </section>
)}
```

Settings page: field "Komisi STIFIn (%)" (number 0–100) memanggil PUT revenue-settings, toast "Pengaturan disimpan".

- [ ] **Step 5: Test → PASS, typecheck, commit**

Run: `pnpm --filter @promotor/platform-api test && pnpm --filter @promotor/platform-api test:integration && pnpm typecheck`

```bash
git add -A
git commit -m "feat(flow): kalkulator omzet & komisi bulanan di header dashboard dengan pengaturan komisi"
```

---

### Task C8: Verifikasi Auto Aftercare D+7 + Salinan Template

**Files:**
- Modify: `apps/platform-api/src/domain/next-action-rules.ts` (judul AFTERCARE lebih aksinya — bila hardcoded)
- Create: `apps/platform-api/src/__tests__/integration/aftercare-d7.integration.test.ts`
- Modify: `packages/promotor-flow-fixtures/src/index.ts` (template AFTERCARE sesuai copy baru, bila perlu)

**Interfaces:**
- Consumes: `calculateAftercareRule` (existing, +7 hari), `bookingService.completeBooking` (membuat NextAction AFTERCARE), `assessmentService` (status tes).
- Produces: **bukti teruji** bahwa 7 hari setelah booking tes selesai, kartu tugas muncul di Today Queue dengan judul yang mengarahkan percakapan penerapan hasil.

- [ ] **Step 1: Tulis test integrasi yang membuktikan perilaku**

```ts
// apps/platform-api/src/__tests__/integration/aftercare-d7.integration.test.ts
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { withIntegrationDb } from './test-env';
import { calculateAftercareRule } from '../../domain/next-action-rules';

describe('auto aftercare D+7', () => {
  it('booking COMPLETED membuat NextAction AFTERCARE due tepat +7 hari dengan idempotency benar', async () => {
    await withIntegrationDb(async (db) => {
      const { sessionToken, bookingId, contactId } = await seedCompletedBookingFixture(db);
      // completeBooking dipanggil dalam fixture; verifikasi next action:
      const actions = await requestOperator(db, 'GET', '/api/v1/flow/next-actions?status=PENDING', sessionToken);
      const aftercare = actions.body.actions.find((a: any) => a.actionType === 'AFTERCARE' && a.contactId === contactId);
      assert.ok(aftercare, 'kartu aftercare harus ada di antrean');

      const expected = calculateAftercareRule(new Date(/* completedAt fixture */), bookingId);
      assert.equal(new Date(aftercare.dueAt).toISOString(), expected.dueAt.toISOString());
      assert.equal(aftercare.idempotencyKey, expected.idempotencyKey);
      assert.match(aftercare.title, /penerapan hasil tes|aftercare|dampak/i);
    });
  });

  it('aftercare tidak bisa diselesaikan sebelum due (temporal guard)', async () => {
    await withIntegrationDb(async (db) => {
      const { sessionToken, aftercareActionId } = await seedCompletedBookingFixture(db);
      const res = await requestOperator(db, 'POST', `/api/v1/flow/next-actions/${aftercareActionId}/aftercare-complete`, sessionToken,
        { outcome: 'HAS_QUESTION', outcomeNotes: 'masih terlalu dini' });
      assert.equal(res.status, 400);
      assert.equal(res.body.error.code, 'VALIDATION_ERROR');
    });
  });
});
```

- [ ] **Step 2: Jalankan. Bila PASS semua → task selesai (fitur sudah ada); bila FAIL, perbaiki sesuai failure**

Kemungkinan gap & perbaikan:
1. Judul AFTERCARE generik (mis. "Aftercare booking") → ubah generator judul di `next-action-rules.ts` / booking-service menjadi: `'Tanyakan penerapan hasil tes di rumah/sekolah (D+7)'`.
2. `GET /next-actions?status=PENDING` belum mendukung filter → tambah param (pola `ListActionsQuerySchema`).
3. Aftercare hanya dibuat dari booking completion — ini sesuai spesifikasi (hasil tes diserahkan saat sesi selesai). TIDAK menambah trigger kedua dari `contact_assessments` (YAGNI; assessment status terlihat di kartu kontak).

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "test(flow): bukti otomatis aftercare D+7 muncul di Today Queue + judul tugas aksinya"
```

---

## Self-Review (dijalankan setelah penulisan)

1. **Cakupan spesifikasi Flow:** post-WA outcome modal (C1) ✓, kanban pipeline (C6) ✓, quick notes timestamped (C2) ✓, sinkron kalender (C3) ✓, persona template (C4) ✓, filter cerdas (C5) ✓, kalkulator omset/komisi (C7) ✓, auto aftercare D+7 (C8 — verifikasi fitur existing) ✓.
2. **Placeholder:** semua langkah konkret; penanda "sesuaikan import/context dengan file existing" merujuk file yang harus dibaca executor, bukan kekosongan desain.
3. **Konsistensi tipe:** `ContactWaOutcome`, `resolveOutcomeEffect`, `RevenueSummary`, `CalendarEvent`, `PIPELINE_STAGES` didefinisikan sekali dan dipakai konsisten di test + UI; nama endpoint konsisten (`/revenue-summary`, `/revenue-settings`, `/contacts/:id/notes`).
